[project]
name = "dress-orbit"
version = "0.1.0"
description = "Dress contributor orbit analysis via GitHub API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "loguru>=0.7.2",
  "tqdm>=4.66.0",
]

[tool.uv]
package = false
